# 회차 029-D-2c-after-2-fix2 — ground truth dump (디버그)
# DEBUG_DUMP_GROUND_TRUTH=1 → chat 응답마다 concat wav + sidecar JSON 을
#   /home/afterlife/afterlife-server/testbed/debug-dump/ 로 보존.
# mac sync-mirror 가 가져가서 _local_merge_groundtruth.sh 로 mp4 와 ffmpeg merge.
[Service]
Environment=DEBUG_DUMP_GROUND_TRUTH=1
Environment=DEBUG_DUMP_DIR=/home/afterlife/afterlife-server/testbed/debug-dump
