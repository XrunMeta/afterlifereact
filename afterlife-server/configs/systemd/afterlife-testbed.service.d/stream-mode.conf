# 회차 029-D-2c — MuseTalk stream 모드 토글
# 1: /infer 호출 시 stream=true 추가 → publisher 로 실시간 frame 푸시
# 0 또는 unset: 기존 batch 모드 (mp4 파일만)
[Service]
Environment=MUSETALK_STREAM_MODE=1
Environment=REALTIME_PUBLISHER_URL=http://127.0.0.1:8400
