[Service]
# 029-D-3b — musetalk 을 GPU 1 단독 분리
#  trap 7 (ollama vs musetalk SM 경합) 해소 + 49GB 메모리 단독 확보.
#  ollama 는 GPU 0 default 그대로 (ollama-afterlife.service 별도).
#  publisher 는 aiortc 라 GPU 무관.
Environment=CUDA_VISIBLE_DEVICES=1
