# 회차 029-D-2c — MuseTalk frame stream mode
# /infer { stream: true } 호출 시 publisher (port 8400) 로 frame push
[Service]
Environment=MUSETALK_PUBLISHER_URL=http://127.0.0.1:8400
Environment=MUSETALK_STREAM_JPEG_Q=80
