# 회차 029-D-2c-after-2-fix2: audio buffer 확장
# 기본 200 frames × 960 samples = 4s buffer 면 testbed 가 wav 30s+ push 시 oldest drop
# → 마지막 4s 만 남아 음성 짤림. 100s 까지 보존하도록 5000 으로 확장.
# (메모리: 5000 × 960 × 2 bytes = 9.6 MB, 허용 범위)
[Service]
Environment=PUBLISHER_AUDIO_QUEUE_MAX=5000
