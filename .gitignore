node_modules/
.DS_Store
.env
.env.local
dist/
build/
*.log
.claude/
.wrangler/
.bkit/
.superpowers/

# 민감정보 (수동 메모) — 평문 시크릿/토큰/복구코드 포함
docs/TOTP 등록.txt
docs/관리.txt
docs/*.local.md
docs/*.local.txt
afterlifeadmin/package-lock.json

# afterlife server — SSL keys/certs (NEVER commit)
afterlife-server/configs/*.key
afterlife-server/configs/*.pem
afterlife-server/configs/*.crt
afterlife-server/configs/*ssl*
afterlife-server/configs/afterlifesslkey.txt
afterlife-server/configs/secret*

# afterlife server — sync mirror (모델/대용량) + 회차 로그
afterlife-server/sync-mirror/
afterlife-server-log/

# sync mirror — 어디 있든 (루트, Reffer/ 하위 등 가비아 동기화 미러 전체)
sync-mirror/

afterlife-server/testbed/data/

# python 캐시 (pytest/py_compile 산출)
__pycache__/
*.pyc

# python venv
afterlife-server/prethird/.venv/

# 로컬 백업·아카이브 (커밋 금지)
.backup/
afterlife-servers.zip

# 음색 카탈로그 변환 산출물 (바이너리, 커밋 제외)
afterlifeapi/seeds/voice_catalog/out/

# T-067 얼굴 임베딩 모델 변환 작업물 (venv/중간산출물, 커밋 제외 — 결과 tflite만 afterlifeRN/assets/models에 커밋)
afterlife-server/scripts/.t067-model/

# T-067 Task 8 픽스처 — 사진(.jpg)만 제외, manifest.json은 감사추적(인물↔샷↔원본 매핑)용 커밋
afterlife-server/fixtures/t067-faces/**/*.jpg

# CosyVoice2 PoC 벤치 산출물(로컬 전용, 커밋 제외) — T-120 cosyvoice
bench_listen_9075/
afterlife-server/cosyvoice/**/*.wav

# RN 로컬 빌드 아카이브 (대용량 바이너리, 커밋 금지)
afterlifeRN/*.zip
afterlifeRN/*.7z

# T-120 fifth PoC 렌더 출력물 (바이너리 산출물, 커밋 제외 — 스크립트만 커밋)
afterlife-server/fifth-poc-output/

# 음색 카탈로그 원본 오디오 (대용량, 커밋 제외)
docs/voices/

# 로컬 도구 작업 디렉터리
.claire/

# 학습/평가 데이터셋 (KsponSpeech 등 대용량, 커밋 금지 — 로컬 전용)
/data/

# 로컬 빌드 APK/AAB (수백MB 대용량, 커밋 금지 — EAS·gradle 산출물)
*.apk
*.aab

# 홈페이지는 별도 저장소(XrunMeta/afterlifehome) — nested git repo, 여기서 추적 금지
/afterlifehome/

# E2E 자동화 실행 산출물 (실패 시 trace/스크린샷 — 커밋하지 않는다)
e2e-automation/runs/

# E2E 테스트 계정 자격증명 (T-454, Task 9 로 afterlifeRN/e2e/ 에서 이관)
e2e-automation/.e2e-credentials.json

# 로컬 D1 스냅샷·백업 (PII 평문이 들어올 수 있다 — 절대 커밋 금지)
# 폴더째 막으면 git 이 하위를 훑지 않아 아래 재포함이 동작하지 않는다.
d1-snapshots/*
!d1-snapshots/README.md
# 위치가 어긋나도 잡히도록 개별 패턴으로 이중 방어.
# migrations/*.sql 은 커밋 대상이므로 *.sql 을 통째로 막지 않는다.
*-dump.sql
*.sql.dump
d1-dump*.sql
